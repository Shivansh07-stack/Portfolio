"use client"; import React, { createContext, useContext, useState } from 'react'; export type Persona = 'Data Scientist' | 'Entrepreneur' | 'Student' | null; interface PersonaContextType { persona: Persona; setPersona: (p: Persona) => void; }; const PersonaContext = createContext<PersonaContextType | undefined>(undefined); export const PersonaProvider = ({ children }: { children: React.ReactNode }) => { const [persona, setPersona] = useState<Persona>(null); return ( <PersonaContext.Provider value={{ persona, setPersona }}> {children} </PersonaContext.Provider> ); }; export const usePersona = () => { const context = useContext(PersonaContext); if (!context) throw new Error('usePersona must be used within PersonaProvider'); return context; };
